# SECURITY WARNING: Never commit .env to git.
# This is a template. Copy this to .env and fill in the values.

# Secret key for JWT signing. Generate a strong random string (e.g., `openssl rand -hex 32`)
SECRET_KEY=dev_secret_change_me_in_prod

# JWT Algorithm (Default: HS256)
ALGORITHM=HS256

# Token Expiry (Minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=480

# Database URL (Default: SQLite local file)
# For PostgreSQL: postgresql://user:password@localhost/dbname
DATABASE_URL=sqlite:///./cctv.db

# Optional: Discord Webhook for notifications
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
